import { StrictMode } from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <StrictMode>
    <strong>hi</strong>
  </StrictMode>,
  document.getElementById('root'),
);
